# Docs Hotfix Validation Plan: v2026.5.3 to v2026.5.3-1

## Scope

- Source repo: `/Users/mudrii/src/open_claw/openclaw`
- Docs repo: `/Users/mudrii/src/open_claw/openclaw-docs`
- Baseline docs release: `openclaw-docs` tag `v2026.5.3`
- Target upstream release: OpenClaw `v2026.5.3-1`
- Release policy: document published stable releases only. `v2026.5.4-beta.1` and `v2026.5.4-beta.2` are prereleases and remain out of the stable docs snapshot.

## Evidence

- `gh release list --repo openclaw/openclaw --limit 12` reports `v2026.5.3-1` as the newest non-prerelease release, published `2026-05-04T09:35:36Z`.
- `gh release view v2026.5.3-1 --repo openclaw/openclaw` describes a core npm hotfix for `openclaw@2026.5.3-1` on the `beta` dist-tag.
- `git diff --name-only v2026.5.3..v2026.5.3-1` in the source repo shows only release workflow metadata, `CHANGELOG.md`, `package.json`, `extensions/memory-lancedb/package.json`, generated config schema, and `src/security/skill-scanner.*`.
- `git log --oneline v2026.5.3..v2026.5.3-1` shows 5 commits.
- GitHub releases and web search show newer `v2026.5.4-beta.*` releases only as prereleases.

## Assumptions

- The docs repo should create a new docs release tag `v2026.5.3-1` rather than documenting the `v2026.5.4` prerelease line.
- Because the upstream hotfix touches `src/security/skill-scanner.*`, the only deep analysis pages that need content-level updates are the security/plugin install scanner references.
- Existing `v2026.5.3` feature summaries remain correct because `v2026.5.3-1` is a narrow correction build, not a new feature release.

## Execution Plan

1. Update release metadata.
   - Files: `README.md`, `CHANGELOG.md`, `ARCHITECTURE.md`, `AGENT_README.md`, `AGENTS.md`.
   - Verify: top-level docs metadata names `v2026.5.3-1` and still preserves the `v2026.5.3` release-window summary.

2. Add a hotfix changelog entry.
   - Files: `CHANGELOG.md`, `README.md`.
   - Verify: the newest changelog section describes the install scanner false-positive fix, package/version refresh, and release-workflow lane filtering without adding prerelease-only `v2026.5.4` behavior.

3. Update affected analysis docs.
   - Files: `analysis/security-plugins.md`, `analysis/security-web-browser.md`.
   - Verify: scanner tables and release notes mention the `v2026.5.3-1` behavior that official bundled plugin package scans no longer treat distant `process.env` access plus API sends in the same compiled bundle as a blocking finding.

4. Restore stable docs pages already referenced by navigation.
   - Files: `docs/automation/tasks.md`, `docs/automation/taskflow.md`, `docs/cli/memory.md`, `docs/cli/tasks.md`, `docs/cli/tui.md`, `docs/concepts/dreaming.md`, `docs/concepts/memory.md`, `docs/concepts/memory-builtin.md`, `docs/tools/web.md`, `docs/tools/web-fetch.md`, `docs/tools/media-overview.md`, `docs/tools/image-generation.md`, `docs/tools/music-generation.md`, `docs/tools/video-generation.md`, `docs/web/tui.md`, `docs/concepts/agent-loop.md`.
   - Verify: every root-relative path listed in `docs/docs.json` exists in `docs/`.

5. Patch stable doc gaps found during review.
   - Files: `docs/gateway/security/index.md`, `docs/cli/index.md`, `docs/help/faq.md`, `docs/channels/slack.md`, `docs/plugins/file-transfer.md`, `docs/plugins/manifest.md`, `docs/providers/index.md`.
   - Verify: file-transfer docs describe both node command allowlists and plugin path policy; Slack progress docs list released subkeys; manifest docs list provider catalog/routing metadata; provider index names bundled stable providers that lack dedicated pages.

6. Record validation commands.
   - Run `git diff --check`.
   - Run markdown-link-oriented greps for stale `Latest published upstream release: v2026.5.3` and top-section drift.
   - Verify: no docs mention `v2026.5.4` as stable.

7. Commit, tag, push, and publish docs release.
   - Commit intended docs files only.
   - Tag `v2026.5.3-1`.
   - Push branch and tag to `origin`.
   - Create a GitHub release in `openclaw-docs` summarizing the hotfix validation.
