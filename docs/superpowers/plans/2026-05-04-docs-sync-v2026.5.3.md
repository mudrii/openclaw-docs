# Docs Sync Plan: v2026.5.2 to v2026.5.3

## Scope

- Source repo: `/Users/mudrii/src/open_claw/openclaw`
- Docs repo: `/Users/mudrii/src/open_claw/openclaw-docs`
- Target source baseline: upstream GitHub release `v2026.5.3`
- Release metadata: non-draft, non-prerelease GitHub release published 2026-05-04 07:01:29 UTC
- Source window: `v2026.5.2..v2026.5.3`
- Commit count: 561 commits
- Diff size: 1,613 files changed, 65,755 insertions, 11,361 deletions

## Success Criteria

1. Fetch and fast-forward both local repos.
   - Verify: `openclaw` has tag `v2026.5.3`; `openclaw-docs` starts clean on `origin/main`.
2. Confirm release authority.
   - Verify: `gh release view v2026.5.3 --repo openclaw/openclaw` reports non-draft, non-prerelease release published 2026-05-04 07:01:29 UTC.
   - Verify: source tag `v2026.5.3` resolves to commit `9c373aed44553b264980ece56bc97bd56820f566`.
3. Refresh measured inventory.
   - Verify: `v2026.5.3` has 14,036 `.ts`/`.tsx` files under `src/`, `extensions/`, `ui/`, `test/`, and `scripts/`.
   - Verify: `v2026.5.3` has 125 top-level `extensions/` directories, 119 extension package manifests, and 87 released `SKILL.md` entrypoints.
4. Update docs release surfaces.
   - Verify: `README.md`, `CHANGELOG.md`, `ARCHITECTURE.md`, `AGENT_README.md`, and `AGENTS.md` identify `v2026.5.3` as the current stable baseline.
   - Verify: `CHANGELOG.md` has a new top release summary for `v2026.5.3`.
5. Add missing release-linked portal pages.
   - Verify: `docs/concepts/progress-drafts.md`, `docs/concepts/streaming.md`, `docs/concepts/queue.md`, `docs/concepts/queue-steering.md`, `docs/tools/steer.md`, `docs/tools/btw.md`, and `docs/plugins/reference/google-meet.md` exist and match the released source docs where copied.
6. Publish docs.
   - Verify: docs commit is pushed to `origin/main`.
   - Verify: docs GitHub release `v2026.5.3` exists and points at the pushed docs commit.

## Update Set

- Add `v2026.5.3` release summary and top-level version metadata.
- Import release-linked docs for progress drafts, queue steering, top-level `/steer`, `/side`/`/btw`, and Google Meet reference.
- Update docs navigation for progress drafts, queue steering, and steer.
- Refresh analysis document headers from `v2026.5.2` to `v2026.5.3`.

## Residual Known Gap

The docs portal mirror is still intentionally partial relative to the upstream `docs/` tree. This pass only adds pages directly required by the `v2026.5.3` release surface and does not attempt a full 608-page mirror import.
