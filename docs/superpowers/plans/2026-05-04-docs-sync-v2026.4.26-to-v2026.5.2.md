# Docs Sync Plan: v2026.4.26 to v2026.5.2

Date: 2026-05-04

## Scope

Validate `openclaw-docs` against the latest published stable OpenClaw release, not unreleased `main` behavior.

- Source repo: `/Users/mudrii/src/open_claw/openclaw`
- Docs repo: `/Users/mudrii/src/open_claw/openclaw-docs`
- Previous docs baseline: `v2026.4.26`
- Target source baseline: upstream GitHub release `v2026.5.2`
- Upstream release publication: 2026-05-02 23:37:55 UTC
- Source window: `v2026.4.26..v2026.5.2`
- Window size: 3,289 commits

## Assumptions

- Stable release docs should not document `Unreleased` or beta-only behavior as released behavior.
- `v2026.5.3-beta.*` and `Unreleased` findings are tracked as follow-up candidates only.
- Broad module analysis files should receive targeted release deltas and metadata updates; they should not be fully rewritten unless a factual error is confirmed.
- The docs repo intentionally mirrors some upstream docs pages but is not a byte-for-byte copy of the upstream `docs/` tree.

## Verification Plan

1. Update repositories.
   - Verify: `git fetch --all --prune`; `openclaw/main` fast-forwarded to `upstream/main`; `openclaw-docs/main` up to date with `origin/main`.

2. Establish release truth.
   - Verify: `gh release view v2026.5.2 --repo openclaw/openclaw` reports non-draft, non-prerelease release published 2026-05-02 23:37:55 UTC.
   - Verify: source tag `v2026.5.2` is signed/annotated and resolves to commit `8b2a6e57fef6c582ec6d27b85150616f9e3a7ba4`.

3. Measure current source inventory.
   - Verify: `v2026.5.2` has 13,950 `.ts`/`.tsx` files under `src/`, `extensions/`, `ui/`, `test/`, and `scripts/`.
   - Verify: `v2026.5.2` has 125 top-level `extensions/` directories, 119 extension package manifests, and 88 released `SKILL.md` entrypoints.

4. Review release changelog and docs baseline.
   - Verify: docs `README.md`, `CHANGELOG.md`, and `ARCHITECTURE.md` mention `v2026.5.2` as the current validated stable baseline.
   - Verify: docs changelog contains a new top release summary for `v2026.5.2`.

5. Split domain review across subagents.
   - Channels/streaming: Discord, Slack, Telegram, Matrix, Mattermost, Microsoft Teams, Feishu, WhatsApp, Google Chat, IRC.
   - Plugins/tools: install/update lifecycle, ClawHub, official external plugins, file transfer, tool policy.
   - Gateway/config/CLI: startup, doctor, proxy, local models, onboarding, security.
   - Providers/media/memory: OpenAI/Codex, OpenRouter, Anthropic, media generation, costs, memory/cron.
   - Verify: findings cite source/changelog evidence and target docs paths; unreleased-only items are not merged into released docs.

6. Apply focused documentation changes.
   - Release metadata and stats in `README.md`.
   - New `v2026.5.2` section in `CHANGELOG.md`.
   - Architecture metadata and extension inventory updates.
   - Plugin CLI lifecycle notes for npm-first cutover, ClawHub explicit specs, dependency state, registry refresh, update, and git installs.
   - Feishu `blockStreaming` default corrected to `false`.
   - OpenAI/Codex docs corrected so native Codex subscription setup uses `openai/gpt-*` with `agentRuntime.id: "codex"` while `openai-codex/*` remains PI OAuth compatibility.
   - Add file-transfer plugin reference page and nav link.
   - Second-pass correction: add stable `v2026.4.29` highlights that sit inside the `v2026.4.26..v2026.5.2` window, including restrictive tool-profile behavior, SDK artifact RPCs, queue steering defaults, `messages.visibleReplies`, NVIDIA provider support, and Memory Wiki people/provenance updates.

7. Validate docs integrity.
   - Verify: no stale current-baseline references to `v2026.4.26` remain in top-level current metadata.
   - Verify: new page is referenced in `docs/docs.json`.
   - Verify: markdown changes are scoped and no unrelated files are reformatted.

8. Release docs repo.
   - Commit docs update.
   - Push `main`.
   - Create GitHub release/tag `v2026.5.2` for `openclaw-docs`.

## Follow-Up Candidates Excluded From This Stable Sync

These were found during review but are not documented as released behavior in this pass because they are `Unreleased`, beta-only, or require a larger page set:

- Unified `streaming.mode: "progress"` and Slack `streaming.progress.render: "rich"`.
- `/steer`, `/side`, and `agents.defaults.toolProgressDetail`.
- OpenRouter cache headers and expanded attribution categories.
- Full channel page set for Discord, Telegram, Matrix, Mattermost, Microsoft Teams, WhatsApp, Google Chat, and IRC.
- Dedicated outbound network proxy guide beyond the `openclaw proxy validate` release note.
- Local-model guide rewrite for all OpenAI-compatible proxy behavior.
